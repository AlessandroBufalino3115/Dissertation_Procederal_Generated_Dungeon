using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChunkSystemCommunicator : MonoBehaviour
{// should get the first index and the last to set the 2d space


    //first ofa ll check fi in the main  boudning box
    // then chek in whic he is 
    // have a radius var here how many layers to look for


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
